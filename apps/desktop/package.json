{
  "name": "@mneme/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Electron chat shell — the FIRST client. Spawns the headless Core as a child process and talks to it over stdio (local IPC). Phase 4 adds remote clients without changing CoreApi.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "electron ."
  },
  "dependencies": {
    "@mneme/ipc": "0.1.0",
    "@mneme/core": "0.1.0"
  },
  "devDependencies": {
    "electron": "^30.0.0"
  }
}
