{
  "name": "@mneme/credential",
  "version": "0.1.0",
  "description": "CredentialStore abstraction. Secrets are Core-only and never reach a client (invariant 5). Windows Credential Manager first (scaffold decision #3).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc -p tsconfig.json" },
  "dependencies": {
    "@napi-rs/keyring": "^1.1.6"
  }
}
