{
  "name": "@mneme/provider",
  "version": "0.1.0",
  "description": "Thin AgentProvider contract. Backends are interchangeable executors; the self-improving loop lives in the Host, never here.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc -p tsconfig.json" }
}
